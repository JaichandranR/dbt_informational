{
  config(
    materialized = 'incremental',
    incremental_strategy = 'merge',
    unique_key = 'AssetDetailsVerumIdentifier',
    properties = {
      "partitioning": "ARRAY[CAST(date_trunc('day', date_parse(datewarrantyend, '%Y-%m-%d')) AS varchar)]"
    }
  )
}
